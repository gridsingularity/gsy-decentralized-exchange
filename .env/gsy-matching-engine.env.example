# The matching engine is configured entirely via its compose entrypoint args
# (web3 <storage> 8080 <node> 9944); only logging is env-driven.
RUST_LOG=info

# API key sent as the x-api-key header to the off-chain storage (must match its API_KEY).
API_KEY=
